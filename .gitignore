# Pipeline runtime (owned by section-runner; do not commit)
.pipeline/

# Dependencies
node_modules/

# Env / secrets (names only in docs)
.env
.env.*
!.env.example

# Host-local secret loader (pipeline/runtime; never commit)
scripts/load-secrets.sh

# OS / editor
.DS_Store
*.log
dist/
dist-types/
coverage/
.wrangler/
.dev.vars
*.tsbuildinfo
.tsbuild/

# Local D1-compatible SQLite from pnpm db:migrate (section 1.3)
.data/

# Audit scratch (auditor temp)
.audit/
.phase-audit/

# Playwright (section 1.5 / 8.2) — local run artifacts (paths recorded in evidence)
test-results/
playwright-report/
blob-report/
reports/playwright/
reports/playwright-run.json
# reports/e2e-coverage.html is the section 8.5 offline keystone (committed)
reports/e2e-report-path.txt
