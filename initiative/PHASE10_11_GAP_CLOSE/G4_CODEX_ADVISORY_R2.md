# G4 Codex re-advisory R2 — Phase 10–11 gap close

**Advisory role:** critic only  
**Date:** 2026-08-09  
**Verdict:** **REVISE**  
**Soul intact:** **YES at intent level; NO at execution-contract level**  
**May owner freeze request proceed?** **NO**  

The REVISE fold materially improved the programme: BC-17/18 now exist, fixture proof is separated from dogfood proof, the dogfood URL and 11.9 deploy point are binding, model/audit pins are explicit, execute-go is separated from freeze, and the pack/design snapshots are present with valid checksums. Those repairs do not close the prior advisory. Literal defects remain in both sampled packs, livability is still not exact or internally consistent, the design authority is still partly laptop-bound, the manifest is not freeze-ready, and the supplied G3 evidence does not reproduce as a whole-pack validation.

No product code, run pack, manifest, environment, remote state, build, sync, merge, or deploy was changed. This advisory file is the only authored output.

## Prior MUST_FIX closure audit

| Prior item | Status | R2 finding |
|---|---|---|
| **MF-1 — frozen G4 input** | **RESIDUAL** | `00_CONSTITUTION.md` is still `LOCKED-CANDIDATE`, explicitly awaiting owner constitution approval and dual re-advisory. No owner approval or frozen constitution/manifest hash is recorded. A candidate cannot be the final G4 object under the prior closure condition. |
| **MF-2 — exact runnable livability** | **RESIDUAL** | Livability v2 adds AC and Playwright columns, but many mappings remain non-exact or wrong: S-SUB-LIST claims inventory `O01/O02`, which are event/rooms settings rather than the existing submissions-list `E01`; schedule uses `L*` rather than the existing `I01–I16`; other rows use `H*`, `O*`, `form/public inv`, `portal inv`, `same file`, or proposed IDs instead of exact inventory rows and test titles. It contains no per-soul evidence destination. Its `AC-11.2-A..J` does not match the sampled spec's `AC-11.2-Jxx/SCALE/SEL/SEND/AUTHZ/UI`. The five-second protocol may start after the spinner is hidden, making the bound circular. |
| **MF-3 — FULL-order/page-atlas coverage** | **RESIDUAL** | BC-17 and BC-18 close the checklist-row omission. `PAGE_ATLAS_COVERAGE.md` does not close the rest: it maps routes only to sections and vague visual labels, not to named AC, exact e2e/screenshot, breakpoint/state, QA_CHECKLIST item, and checklist row. Several settings rows are blank; four public routes are collapsed into `11.3 note / already shipped`; the evaluator route is unnamed. It also labels evaluations, speakers, and settings as primary while the taste-score denominator omits them. The constitution now contains **18 distinct soul IDs** after adding S-SCHED-CHROME and S-EVAL-EXPORT, while 11.9 still requires only “All 16 souls evidence”; this can omit BC-17/18 while appearing green. |
| **MF-4 — sampled pack 10.1** | **RESIDUAL** | The source concept is still truncated to `S-SUB-`. The Playwright root is fixed and AC-10.1-A..E were appended, but the traceability row still says `Success metrics / Named tests above`, with no exact test titles or AC-to-test mapping. The plan still uses generic “e2e + unit” and “implement named tests”; UI e2e remains conditional. Pagination/windowing still does not pin response shape, cursor/page contract, stable ordering, filter semantics, or SPA/API compatibility. The generic unrelated CFP/evaluator negative block remains. The dogfood timing proof is not owned in this spec, and the matrix's timing start is not valid. |
| **MF-5 — sampled pack 11.2** | **RESIDUAL** | `AC-11.2-Jxx` is a wildcard, not ten exact regression criteria. The pack does not individually preserve rendered bodies/missing merge-field behavior, delivery log, or ICS UID/SEQUENCE, and it omits loading/empty/error/session-expired, keyboard/focus, and 390px behavior. Field-flow is incorrectly `N/A` for the audience/message/review/send workflow. The security block remains unrelated boilerplate. The plan does not own `KMS-competition/initiative/BROWSER_E2E_INVENTORY.md`, does not name the existing comms test files as modified, has only `Step shell / Audience virtualization / Wire existing APIs / e2e`, and still makes e2e and inventory updates conditional for an all-UI section. |
| **MF-6 — immutable builder-readable Lumen authority** | **RESIDUAL** | The design snapshot and checksum are now present and verify. However, Article VIII, the Phase 11 index, and both sampled specs still cite `/Users/qualitycontrol/Documents/ChatGPT/speakerops/design-pack/` plus an unqualified audit file. Only 10.1 appends the initiative snapshot; 11.2 does not. The manifest gives a laptop path and merely asks preflight to make the snapshot “present on builder path”; it pins no exact `/data/...` source/sync destination or remote digest check. A made-pilot builder can still be forced to improvise. |
| **MF-7 — dogfood proof and DEMO security** | **CLOSED** | The constitution and livability now bind D proof to `https://www.speakerops.org`, record F at 10.7 and D at 11.9, require deploy revision/report provenance, require real session cookies for three roles, and specify allowlisted DEMO host/event plus non-allowlisted rejection. The 10.3 criteria include both fail-closed negatives. This closure does not cure the separate traceability and 11.9 soul-count defects above. |
| **MF-8 — freeze manifest and later fresh go** | **RESIDUAL** | Freeze and execute authorization are now correctly separate, and the model pins, gates, side effects, and basic rollback are much better. The object is still not ready to present for freeze: branch/SHA/worktree and snapshot pins are blank; livability/checklist paths contain `…` rather than canonical absolute paths; `/data/speakerops-build` is still “verify preflight”; no exact remote pack/design sync destination is pinned; preflight does not require the expected base revision/branch match; and no canonical evidence destinations are defined for command, exit, log, `result.json`, report, deploy revision, timestamp, and hashes. The current dirty source tree also has no frozen inventory/exclusion declaration. |
| **MF-9 — G3 validation and consistency evidence** | **RESIDUAL** | Both checksum manifests self-verify. The supplied `validate-run-pack.log` reports 17 individual PASS results, but it does not bind a validator revision, source corpus, manifest hash, pack snapshot hash, or exit status. `G3_VALIDATION.md` references `SECTION_ORDER.proposed.txt`, which is absent from the initiative. Its `/tmp/phase10-11-manifest.json` is not preserved evidence. Re-running the validator over the supplied `packs-snapshot/` with that recorded manifest returns **FAIL: 14 dependency checks failed, 123 passed**, because referenced prior sections 1.x–8.x are absent. Therefore the initiative snapshot cannot reproduce the asserted graph/consistency proof. |

## Remaining MUST_FIX before another G4

1. Make the constitution an owner-approved, hash-pinned G4 input and enumerate the authoritative soul-ID set explicitly; remove the 16-versus-18 ambiguity from 11.9 and every exit gate.
2. Replace livability and page-atlas shorthand with exact AC → named test title/file → exact existing/new inventory ID → `@inv` → F/D evidence path mappings. Correct the inventory families and define one non-circular five-second clock start.
3. Repair 10.1 and 11.2 in their specs **and plans**. Literal acceptance, interface, state, security, I16, file ownership, unconditional E9 gates, and per-AC completion checks must live in the packs, not only in the matrix or this advisory.
4. Make the design authority singular and remote-readable: cite only the checksummed initiative/control-plane snapshot in all authorities and packs, pin its exact made-pilot destination, and verify its digest at preflight.
5. Populate a freeze-ready manifest before asking the owner: immutable source/pack/design/constitution identities; branch/worktree state; exact remote paths and SECTION_ORDER before/after; base-revision and lock preflight; evidence destinations; deploy rollback/recovery.
6. Preserve the validator manifest, exact command/tool revision, exit status, full dependency corpus or declared validated baseline, and SECTION_ORDER evidence inside the initiative. Re-run whole-corpus validation and consistency checks to green, then obtain the required independent re-advisory.

## Verdict

The fold protects the intended product soul better than R1, but a competent builder can still satisfy the sampled packs literally while truncating/filtering the submissions list incompatibly, weakening comms trust-before-send, omitting primary surfaces from taste scoring, or closing 11.9 without the two added residual souls. The current manifest and evidence are therefore not a safe object of owner freeze.

**Final verdict: REVISE.**  
**Owner freeze request: NO.** Owner execute-go remains a separate later authorization and is not assessed as granted.
