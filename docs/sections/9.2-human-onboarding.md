# Section 9.2 — Human onboarding path

> **Status:** implemented · **Standards:** E1–E12 · **Depends:** 9.1  
> **Souls:** **S-ONB-HUMAN** (zero → dogfood checklist timed)  
> **Outline authority:** [0.5-docs-onboarding-outline.md](../governance/0.5-docs-onboarding-outline.md)  
> **Proof keystone:** **9.6** / BC13

## Goal

`docs/ONBOARDING.md` zero→dogfood checklist timed; **S-ONB-HUMAN**. New operator follows numbered steps without tribal knowledge: prereqs → env names → wrangler → migrate → seed → first admin → demo path → e2e → optional Airtable → deploy notes. Timebox target **&lt;90 minutes** for local zero→demo.

## Delivered

| Item | Location |
|------|----------|
| Timed human onboarding prose | [`docs/ONBOARDING.md`](../ONBOARDING.md) |
| Numbered steps 1..15 | same (prereq through hand-off) |
| Env **names** only table | same · cross-ref [SECRETS.md](../SECRETS.md) |
| Demo path (CFP → … → schedule → readiness) | ONBOARDING §8 |
| E2E + inventory commands | ONBOARDING §9 · [E2E.md](../E2E.md) |
| CF / wrangler / optional Airtable | ONBOARDING §10–14 · [OPERATIONS.md](../OPERATIONS.md) |
| Named tests | `tests/governance/9.2-human-onboarding.test.mjs` · `tests/9.2-human-onboarding.test.ts` |
| Section doc | this file |

## In scope (spec)

- Prerequisites
- Cloudflare account steps (names)
- Wrangler
- Migrate + seed
- Env template names
- First admin
- Demo path
- E2E command
- Airtable optional

## Inventory ownership

**None** (API/docs/governance only). No browser `@inv` IDs. CLI/unit tests required.

## Named test assertions

| Assertion | File |
|-----------|------|
| assert ONBOARDING has numbered steps ≥10 | `tests/9.2-human-onboarding.test.ts` · governance 9.2 |
| assert env names table has no values matching sk- or eyJ | same |
| assert demo path mentions CFP and schedule | same |

## AC map

| AC | Proof |
|----|--------|
| Numbered steps 1..N | assert ONBOARDING has numbered steps ≥10 |
| Env names only table | assert env names table has no values matching sk- or eyJ |
| Demo path matches constitution souls | assert demo path mentions CFP and schedule |
| Timebox target e.g. &lt;90m stated | assert ONBOARDING has numbered steps ≥10 (+ timebox regex in tests) |
| Section 9.2 in-scope only (human onboarding path) | this doc + ONBOARDING; no product handlers |
| N/A — no new product HTTP handlers | declared N/A |
| N/A — no new consequential product writes | declared N/A |
| Automated tests pass in `pnpm test:ci` | vitest 9.2 + governance 9.2 |

## N/A with explicit proof

| Generic AC | 9.2 treatment |
|------------|----------------|
| Zod HTTP handlers | No new routes; no OpenAPI changes |
| `audit_events` + `correlationId` on writes | No domain commands / product writes |
| Browser inventory IDs | None owned |
| HTML `reports/onboarding.html` | **9.5** |
| Dry-run evidence BC13 | **9.6** |
| Agent path prose | **9.3** |

## Security (E10)

- Env **names** only in ONBOARDING and this note  
- No API keys, magic-link tokens, `sk-…`, or `eyJ…` JWT values  
- Commands referenced exist in `package.json` / OPERATIONS / CLI  

## Scope guard

- No Next/RSC, OR-Tools, Temporal, dual-write Airtable, agent fleet UI  
- Do not invent endpoints or schema columns  
- Do not shrink REQUIRED inventory  
- Do not weaken cookie flags / CSP for demo convenience  

## Rollback

- Revert git commit for this section  
- Data: N/A (docs only); D1 Time Travel remains documented in [OPERATIONS.md](../OPERATIONS.md)

## Gates

```bash
pnpm typecheck
pnpm test:ci
# No new e2e inventory ownership this section
```

## Phase 9 critical path

`9.1` → **`9.2` (this)** → `9.3` → `9.4` → `9.5` → `9.6`
