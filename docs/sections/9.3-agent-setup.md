# Section 9.3 — Agent setup and CLI runbook

> **Status:** implemented · **Standards:** E1–E12 · **Depends:** 9.1, 7.2  
> **Souls:** **S-ONB-AGENT** (agent zero → CLI ops without human UI except secret inject)  
> **Outline authority:** [0.5-docs-onboarding-outline.md](../governance/0.5-docs-onboarding-outline.md)  
> **Proof keystone:** **9.6** / BC14

## Goal

`docs/AGENT_SETUP.md` + `docs/CLI.md` complete; copy-paste agent prompt block; **S-ONB-AGENT**. Agent follows AGENT_SETUP + OpenAPI + CLI: scoped key → readiness JSON → design publish; deny-scope proof; env **names** only.

## Delivered

| Item | Location |
|------|----------|
| Agent setup prose + prompt block | [`docs/AGENT_SETUP.md`](../AGENT_SETUP.md) |
| CLI runbook (key mint, scopes, exit codes, CLI01–CLI12) | [`docs/CLI.md`](../CLI.md) |
| OpenAPI URL | `GET /openapi.json` · `speakerops openapi` |
| Readiness + design publish | `speakerops reports readiness` · `design publish` |
| Scope deny example | AGENT_SETUP §6 · CLI.md CLI07 |
| Named tests | `tests/governance/9.3-agent-setup.test.mjs` · `tests/9.3-agent-setup.test.ts` |
| Section doc | this file |

## In scope (spec)

- Key mint
- Scopes examples
- Readiness JSON
- Design publish
- Deny examples
- OpenAPI URL
- Exit codes

## Inventory ownership

**None** (API/docs/governance only). No browser `@inv` IDs. CLI/unit tests required (implementation already in 7.2; this section is docs + AC proof).

## Named test assertions

| Assertion | File |
|-----------|------|
| assert AGENT_SETUP contains speakerops reports readiness | `tests/9.3-agent-setup.test.ts` · governance 9.3 |
| assert scope deny example | same |
| assert CLI_INVENTORY referenced | same |

## AC map

| AC | Proof |
|----|--------|
| Copy-paste agent block | assert AGENT_SETUP contains speakerops reports readiness (+ prompt block regex in tests) |
| Scope deny example | assert scope deny example |
| No secret values | assert CLI_INVENTORY referenced (+ secret-pattern scan) |
| Section 9.3 implements only documented in-scope behaviors | this doc + AGENT_SETUP / CLI; no product handlers |
| N/A — no new product HTTP handlers | declared N/A |
| N/A — no new consequential product writes | declared N/A |
| Automated tests pass in `pnpm test:ci` | vitest 9.3 + governance 9.3 |
| No secrets, API keys, or magic-link tokens committed or logged full | assert scope deny example + secret-pattern scan |

## N/A with explicit proof

| Generic AC | 9.3 treatment |
|------------|----------------|
| Zod HTTP handlers | **N/A this section** — no new product HTTP handlers (product law still applies when handlers land elsewhere) |
| `audit_events` + `correlationId` on writes | **N/A this section** — no new consequential product writes (audit law still applies when writes land elsewhere) |
| Browser inventory IDs | None owned |
| HTML `reports/agent-setup.html` | **9.5** |
| Dry-run evidence BC14 | **9.6** |
| CLI binary implementation | **7.2** (dependency; docs expanded here) |

## Security (E10)

- Env **names** only in AGENT_SETUP, CLI.md, and this note  
- No API keys, magic-link tokens, `sk-…`, or `eyJ…` JWT values  
- Commands shown exist in [CLI_INVENTORY.md](../../KMS-competition/initiative/contracts/CLI_INVENTORY.md) / [COMMANDS.md](../../KMS-competition/initiative/contracts/COMMANDS.md)  
- Scope deny documented; server-side enforcement (E2/E8)

## Scope guard

- No Next/RSC, OR-Tools, Temporal, dual-write Airtable, agent fleet UI  
- Do not invent endpoints or schema columns  
- Do not shrink REQUIRED inventory  
- Do not weaken cookie flags / CSP for demo convenience  
- Do not violate constitution soul **S-ONB-AGENT**

## Rollback

- Revert git commit for this section  
- Data: N/A (docs only); D1 Time Travel remains documented in [OPERATIONS.md](../OPERATIONS.md)

## Gates

```bash
pnpm typecheck
pnpm test:ci
# No new e2e inventory ownership this section
```

## Phase 9 critical path

`9.1` → `9.2` → **`9.3` (this)** → `9.4` → `9.5` → `9.6`
