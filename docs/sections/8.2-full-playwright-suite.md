# Section 8.2 — Full Playwright suite

> **Status:** implemented · **Standards:** E1–E12 (`speakerops-engineering-standards.md`) · **Depends:** 8.1  
> **Souls:** **S-E2E-RUN** (full REQUIRED inventory PASS + run report + console-clean happy paths)

## Goal

All non-DEFER **REQUIRED** inventory journeys **PASS** with Playwright `@inv` tags and a stored raw run-report artifact. Close product gaps for cross-cutting state journeys **L01–L04**. No inventory shrink.

## Delivered

| Item | Location |
|------|----------|
| Cross-cutting state journeys | `playwright/e2e/states_cross_cutting.spec.ts` (`@inv:L01`–`L04`) |
| Phase 8 keystone soul path | `playwright/e2e/phase8_full_suite_keystone.spec.ts` |
| Empty / error / loading product polish | `apps/web/src/pages/Submissions.tsx` + `shell.css` (Lumen tokens) |
| Crawl map for new primaries | `scripts/ui-crawl-allowlist.json` (L01 CTA, L02 retry) |
| Inventory statuses L01–L04 | `PASS` in `BROWSER_E2E_INVENTORY.md` |
| Run report wiring | `playwright.config.ts` + `scripts/e2e-run.mjs` |
| Report path manifest | `reports/e2e-report-path.txt` (after `pnpm test:e2e`) |
| JSON run report | `reports/playwright-run.json` (`E2E_PLAYWRIGHT_RUN_REPORT`) |
| HTML raw report | `playwright-report/` (+ mirror `reports/playwright/`) |
| Coverage HTML | `reports/e2e-coverage.html` (copy of HTML index) |
| Evidence | `KMS-competition/initiative/evidence/e2e-full.txt` |
| Named unit assertions | `tests/8.2-full-playwright-suite.test.ts` |
| Governance | `tests/governance/8.2-full-playwright-suite.test.mjs` |

## Named assertions (spec)

| Assertion | Where |
|-----------|--------|
| assert every REQUIRED inventory id status PASS in report JSON | Vitest 8.2 + governance 8.2 (inventory PASS + optional run-report when present) |
| assert no inventory rows deleted in git diff of inventory file | Vitest 8.2 vs `e2e-inventory-required-baseline.json` (anti-shrinkage) |

## Inventory ownership (this section)

| Concern | Owner |
|---------|--------|
| Full suite green + run report (S-E2E-RUN) | **8.2** |
| L01–L04 state journeys | **8.2** (`states_cross_cutting.spec.ts`) |
| L05 large list | 6.3 / 6.4 (already PASS) |
| Prior phase journeys A–K / N / O | Feature sections + phase keystones (already PASS) |
| Inventory completeness crawl | 8.1 |

## Report path stored

| Artifact | Path | Notes |
|----------|------|-------|
| JSON run report | `reports/playwright-run.json` | Phase 8 gate via `E2E_PLAYWRIGHT_RUN_REPORT` |
| Raw HTML | `playwright-report/` | Playwright default HTML reporter folder |
| Mirror HTML | `reports/playwright/` | CI / `E2E_PLAYWRIGHT_HTML_MIRROR=1` |
| Coverage HTML | `reports/e2e-coverage.html` | Law reference path |
| Path manifest | `reports/e2e-report-path.txt` | Written by `e2e-run.mjs` after each run |

Local artifacts under `playwright-report/` and `reports/playwright/` are gitignored (ephemeral). Evidence file records the canonical paths.

## Phase 8 inventory gate

```bash
pnpm test:e2e
E2E_INVENTORY_GATE=phase8 E2E_PLAYWRIGHT_RUN_REPORT=reports/playwright-run.json pnpm test:e2e:inventory
```

Requires: every non-DEFER REQUIRED status **PASS** + Playwright-bound `@inv` + **passed, non-skipped** run-report outcomes.

## N/A with explicit proof (no product HTTP surface this section)

| Generic AC | 8.2 treatment |
|------------|----------------|
| New HTTP handlers + Zod/E4 | **None added** — product UI polish + E2E only |
| Consequential product writes → audit_events | **None added** — tests use existing APIs |
| New product composition roots | **N/A** |
| OpenAPI / command registry | **Unchanged** |

## Security (E10)

- No secrets, API keys, or magic-link tokens in evidence, tests, or reports checked into docs
- Env **names** only: `E2E_WEB_SERVER`, `E2E_PLAYWRIGHT_RUN_REPORT`, `E2E_PLAYWRIGHT_SUITE_REPORT`, `E2E_PLAYWRIGHT_HTML_DIR`, `E2E_PLAYWRIGHT_HTML_MIRROR`, `E2E_INVENTORY_GATE`, `CI`

## Performance notes

- L05 (already PASS): 150-row seed list p95 target &lt; 200ms after warm (documented in readiness e2e)
- Public CFP: skeleton allowed; L03/L04 assert loading resolves without multi-second blank hang

## Console-clean happy paths

L04 + phase8 keystone + foundation smoke collect `pageerror` / `console.error` on admin shell, submissions, forms, and public CFP happy paths.

## Gates proof

```bash
pnpm typecheck
pnpm test:ci
pnpm test:e2e
pnpm test:e2e:inventory
E2E_INVENTORY_GATE=phase8 E2E_PLAYWRIGHT_RUN_REPORT=reports/playwright-run.json pnpm test:e2e:inventory
```

## AC map

| AC | Proof |
|----|--------|
| 100% REQUIRED PASS or constitution DEFER | inventory L01–L04 PASS; full suite green; phase8 gate |
| Report path stored | `reports/e2e-report-path.txt` + evidence `e2e-full.txt` |
| Console-clean happy paths | `@inv:L04` + keystone |
| In-scope full suite only | this doc; no new handlers/writes |
| N/A — no new handlers | declared above |
| N/A — no new product writes | declared above |
| Automated tests pass in `pnpm test:ci` | Vitest 8.2 + governance 8.2 |
| No secrets committed/logged | evidence + artifact review; E10 |

## Related

- Law: [`docs/governance/0.3-e2e-inventory-law.md`](../governance/0.3-e2e-inventory-law.md)
- E2E guide: [`docs/E2E.md`](../E2E.md)
- Completeness audit: [`8.1-inventory-completeness.md`](./8.1-inventory-completeness.md)
- Canonical inventory: `KMS-competition/initiative/BROWSER_E2E_INVENTORY.md`
