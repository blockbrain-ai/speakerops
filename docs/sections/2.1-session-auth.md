# Section 2.1 — Session auth magic link

> **Status:** implemented · **Standards:** E1–E12 · **Depends:** 1.3, 1.4  
> **Soul:** — (supports programme) — hard dependency for later authz / portal / admin

## Goal

Magic-link auth exchanging single-use tokens for HttpOnly Secure session cookies for admin and speaker purposes.

## Delivered

| Item | Location |
|------|----------|
| Migration `0002_auth.sql` | `packages/db/migrations/0002_auth.sql` |
| Drizzle tables | `users`, `authSessions`, `magicLinks` in `packages/db/schema.ts` |
| Shared Zod DTOs | `packages/shared/src/auth.ts` |
| Auth commands + routes | `apps/api/src/modules/auth/*` |
| Composition root | `apps/api/src/index.ts` (`createApp` / `createAppWithAuth`) |
| Login UI | `apps/web/src/pages/Login.tsx` · route `/login` |
| Speaker landing | `apps/web/src/pages/PortalHome.tsx` · route `/portal` |
| Unit/integration tests | `apps/api/src/modules/auth/auth.test.ts` |
| Playwright B01–B03 | `playwright/e2e/auth_magic_link.spec.ts` |
| Dev transport | `MagicLinkTestOutbox` + `GET /api/auth/dev/outbox` (e2e only) |

## HTTP (COMMANDS.md)

| Method | Path | Command | Success |
|--------|------|---------|---------|
| POST | `/api/auth/magic-link` | `Auth.RequestMagicLink` | 200 `{ sent: true }` |
| POST | `/api/auth/exchange` | `Auth.ExchangeMagicLink` | 200 + `Set-Cookie` |
| POST | `/api/auth/logout` | `Auth.Logout` | 204 + clear cookie |

### Cookie flags

`speakerops_session=<token>; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=…`

### Security

- Tokens stored **only** as SHA-256 hex (`token_hash`) — never plaintext in DB
- Single-use magic links (`used_at`); replay → 401 `UNAUTHORIZED`
- Unknown email still returns `{ sent: true }` (no enumeration)
- Never log magic-link or session tokens (E10)
- Consequential writes → `audit_events` with `correlationId` (E3)
- Zod validation + E4 envelopes on all auth handlers (E4)

## Schema (SCHEMA.md)

### users
`id, email, name, created_at, updated_at` (+ unique index on email)

### auth_sessions
`id, user_id, token_hash, expires_at, created_at`

### magic_links
`id, user_id, event_id NULL, purpose, token_hash, expires_at, used_at, created_at`

## Inventory

| ID | Status | Proof |
|----|--------|-------|
| B01 | IMPLEMENTED | `@inv:B01` admin login + HttpOnly cookie |
| B02 | IMPLEMENTED | `@inv:B02` speaker single-use + replay 401 |
| B03 | IMPLEMENTED | `@inv:B03` logout clears cookie |

## Named test assertions

| Assertion | File |
|-----------|------|
| magic link token stored only as hash | `apps/api/src/modules/auth/auth.test.ts` |
| reused exchange returns 401 | same |
| Set-Cookie contains HttpOnly | same + Playwright B01/B03 |
| unknown email still returns sent:true | same |

## Dev transport

Local e2e (`scripts/e2e-api-server.mjs`) uses `createAppWithAuth({ enableDevOutbox: true })`.

- Captures plaintext tokens **in process memory only** (never DB)
- `GET /api/auth/dev/outbox?email=` returns last token for Playwright
- **Not** registered on production default export (`enableDevOutbox: false`)

## Durable email delivery (dogfood / production)

Magic-link **email** uses the transactional outbox (E7) — not request-path HTTP:

1. `Auth.RequestMagicLink` stores hash in D1, encrypts plaintext (`AUTH_LINK_ENCRYPTION_KEY`), inserts `outbox_events` topic `auth.magic_link`.
2. Queue consumer / cron drains via `processAuthMagicLinkOutbox` → Cloudflare Email Sending (`EMAIL` binding or REST) or Resend.
3. After send, payload is **redacted** (no ciphertext left in D1).

**Allowlist:** when `MAGIC_LINK_ALLOWLIST` is non-empty under controlled policy, only listed emails may **self-register** (create a user). Existing **provisioned members** (e.g. accepted speakers whose membership was created by `Decision.Record`) re-enter regardless of the allowlist (`programReentry`, membership-aware). Always returns `{ sent: true }` (no enumeration).

**Local e2e** still uses in-memory `MagicLinkTestOutbox` + optional `AUTH_DEV_OUTBOX` (no real provider).

## Out of scope

- OAuth / OIDC / passwords
- Event membership roles → **2.2** (implemented)
- Auth guards on `/admin` (B04+) → **2.2** (implemented)

## Env names only (E10)

| Name | Role |
|------|------|
| `AUTH_DEV_OUTBOX` | Documented for e2e server; never commit values |
| `SESSION` cookie name | `speakerops_session` (code constant, not secret) |
| `APP_PUBLIC_BASE_URL` | Login link origin |
| `AUTH_LINK_ENCRYPTION_KEY` | Encrypt outbox payload |
| `AUTH_EMAIL_PROVIDER` / `EMAIL_PROVIDER` | `cloudflare` \| `resend` |
| `AUTH_EMAIL_FROM` / `EMAIL_FROM` | From: address |
| `CLOUDFLARE_EMAIL_API_TOKEN` | REST Email Sending (if no binding) |
| `MAGIC_LINK_ALLOWLIST` | Comma-separated tester emails |

## Gates

```bash
pnpm typecheck
pnpm test:ci
pnpm test:e2e            # includes B01–B03 when web server on
pnpm test:e2e:inventory  # B01–B03 IMPLEMENTED require @inv tags
pnpm db:migrate          # applies 0002_auth.sql
```

## Commit

`feat(2.1): session-auth-magic-link`
