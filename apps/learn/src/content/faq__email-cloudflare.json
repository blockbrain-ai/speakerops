{
  "id": "L24",
  "path": "/faq/email-cloudflare",
  "title": "Cloudflare email delivery",
  "bodyHtml": "<h1>Cloudflare email delivery</h1>\n<p class=\"learn-lead\">How demo email sending works on Cloudflare and how to debug missing messages.</p>\n<section class=\"learn-section\">\n<h2 id=\"demo-email-path\">Demo email path</h2>\n<p>The private demo uses Cloudflare Email Sending with From addresses such as SpeakerOps &lt;noreply@speakerops.org&gt;. EMAIL_PROVIDER and AUTH_EMAIL_PROVIDER are set to cloudflare. The Worker send_email binding named EMAIL is preferred over REST fallback tokens.</p>\n<p>Magic links and comms are enqueued to outbox tables and drained by queue consumers and cron. The login HTTP request does not wait on provider round-trips.</p>\n<p>Cloudflare Email Sending delivers plain messages with <strong>no attachments</strong>. Calendar invites are therefore not attached to demo emails: speakers download the <code>.ics</code> from the portal (“Add to calendar (.ics)” on a placed session). The Resend adapter supports attachment delivery when configured on other environments.</p>\n</section>\n<section class=\"learn-section\">\n<h2 id=\"operator-debug-steps\">Operator debug steps</h2>\n<p>Confirm domain email sending is enabled for speakerops.org. Confirm SPF/DKIM/DMARC records present from onboarding. Confirm Worker bindings and vars. Confirm queue consumers are deployed and not erroring. Ask recipients to check spam.</p>\n<ol class=\"learn-steps\">\n<li>Verify EMAIL_PROVIDER=cloudflare on the demo env.</li>\n<li>Confirm send_email binding EMAIL exists on the Worker.</li>\n<li>Request a single magic link and wait for drain (seconds to a minute under load).</li>\n<li>If nothing arrives, inspect worker logs for drain errors (redact secrets).</li>\n<li>Confirm the recipient is an existing user or provisioned member — unknown emails are not issued login links.</li>\n</ol>\n<aside class=\"learn-callout learn-callout--note\" data-testid=\"learn-callout\"><p><strong>NOTE.</strong> Resend is not used on the demo host. Other environments may set EMAIL_PROVIDER=resend with RESEND_API_KEY.</p></aside>\n</section>\n<section class=\"learn-section\">\n<h2 id=\"local-development\">Local development</h2>\n<p>Local and CI often use sandbox provider behavior or AUTH_DEV_OUTBOX for Playwright. That path is intentional for tests and must not be confused with production Cloudflare delivery.</p>\n</section>"
}
