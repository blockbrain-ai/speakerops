{
  "id": "L13",
  "path": "/program/comms",
  "title": "Comms and calendar",
  "bodyHtml": "<h1>Comms and calendar</h1>\n<figure class=\"learn-figure\">\n  <img src=\"/assets/images/hero-comms-campaign.webp\" alt=\"Campaign wizard: Audience → Message → Review → Send\" width=\"1200\" loading=\"lazy\" decoding=\"async\" />\n  <figcaption>Campaign wizard: Audience → Message → Review → Send</figcaption>\n</figure>\n<p class=\"learn-lead\">Draft templated email, preview recipients, send via outbox, and manage calendar invites — with Campaign vs History surfaces so ops never crowd the wizard.</p>\n<section class=\"learn-section\">\n<h2 id=\"where-comms-live\">Where comms live</h2>\n<p>Open Admin → Comms. The page has two surfaces:</p>\n<ul>\n<li><strong>Campaign</strong> (default) — the send wizard only: Audience → Message → Review → Send. Delivery log and calendar-invite tools are <em>not</em> on this surface.</li>\n<li><strong>History</strong> — delivery log (jobs, recipients, status) and calendar invite generate/update for scheduled sessions.</li>\n</ul>\n<p>Deep-link with <code>?surface=history</code> when you need the log or ICS panels. Send still enqueues outbox work for asynchronous drain — the request path does not call the email provider directly.</p>\n</section>\n<section class=\"learn-section\">\n<h2 id=\"campaign-wizard\">Campaign wizard</h2>\n<figure class=\"learn-figure\">\n  <img src=\"/assets/images/hero-comms-history.webp\" alt=\"History surface: delivery log and calendar invite tools\" width=\"1200\" loading=\"lazy\" decoding=\"async\" />\n  <figcaption>History surface: delivery log and calendar invite tools</figcaption>\n</figure>\n<ol class=\"learn-steps\">\n<li>On <strong>Campaign</strong>, set audience (status, search, or explicit selection). Next needs at least one recipient.</li>\n<li><strong>Message</strong> — edit subject and body (merge fields like <code>{{name}}</code>). Click <strong>Save template</strong> before Next; the step stays blocked with “Save template before review” while unsaved.</li>\n<li><strong>Review</strong> — run preview; inspect recipients, bodies, and missing fields. Editing audience or message invalidates a prior preview.</li>\n<li><strong>Send</strong> (last step) — optional attach of an existing calendar invite, then Send. Trust-before-send: Send stays disabled without a valid matching preview.</li>\n</ol>\n</section>\n<section class=\"learn-section\">\n<h2 id=\"draft-and-preview\">Draft and preview</h2>\n<p>Preview requires a saved <code>templateId</code> on the server. Inspect missing fields before any send. Fix person or session data when required tokens are blank. Calendar invites (ICS with stable UID/SEQUENCE) are <strong>generated on History</strong>; on the hosted demo, email carries no attachments and speakers download the .ics from the portal instead. On Send you may <strong>attach</strong> an already-generated invite to this campaign.</p>\n<ol class=\"learn-steps\">\n<li>Open Admin → Comms (Campaign).</li>\n<li>Audience → Message → Save template → Review → Run preview.</li>\n<li>Repair data or template tokens until missing fields are acceptable.</li>\n<li>Send, then open History for the delivery log if you need job detail.</li>\n</ol>\n</section>\n<section class=\"learn-section\">\n<h2 id=\"scopes-and-cli\">Scopes and CLI</h2>\n<p>comms:draft allows preview automation. comms:send is default-deny on new API keys and required for send. Agents should draft and show humans a preview before any key with send is used in production.</p>\n<pre class=\"learn-code\" data-testid=\"learn-code-block\"><code>speakerops comms templates --event &lt;eventId&gt; --json\nspeakerops comms draft --template &lt;templateId&gt; --preview --json\nspeakerops comms send --preview-id &lt;previewId&gt; --json</code></pre>\n</section>\n<section class=\"learn-section\">\n<h2 id=\"delivery-backends\">Delivery backends</h2>\n<p>Local and many dev paths use sandbox provider behavior. Demo uses Cloudflare Email Sending, which delivers no attachments — calendar .ics files reach speakers as a portal download. Other environments may use Resend (attachment-capable) when EMAIL_PROVIDER=resend and secrets are set. Operators should know which provider the current host uses before debugging inbox issues.</p>\n<aside class=\"learn-callout learn-callout--warning\" data-testid=\"learn-callout\"><p><strong>WARNING.</strong> Never put raw API keys or magic links into template bodies. Prefer product tokens and portal links that require auth.</p></aside>\n</section>\n<aside class=\"learn-product-routes\"><h2>Where this lives in the app</h2><ul>\n<li><code>/admin/comms</code></li>\n</ul><p class=\"learn-muted\">Open these routes on your demo or local host while you follow the steps above.</p></aside>"
}
