/**
 * Capture extra Learn Phase-3 hero shots via local e2e stack.
 * Usage: E2E_WEB_SERVER=1 already running OR run under e2e-run.
 * Prefer: node scripts/e2e-run.mjs with a thin playwright file.
 */
console.log('use playwright e2e instead');
